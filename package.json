{
  "name": "audit-project",
  "version": "1.0.0",
  "description": "Read-only audit skill for an in-progress repo in any stack: parallel lenses, a skeptic per finding, an anchor that runs the repo's own commands, and a handoff package.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "lint": "node --check scripts/detect.mjs && node --check scripts/anchor.mjs && node --check scripts/handoff.mjs && node --check scripts/tarjetas.mjs"
  }
}
